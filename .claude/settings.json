{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "condition": "toolInput.command.includes('git push')",
        "prompt": "Before pushing, run /simplify to review the changes for reuse, simplification, efficiency, and altitude issues. Continue with the push only after applying the cleanup fixes."
      }
    ]
  }
}
